defaultTasks 'zip'

task zip << {
	zip.zipname = 'marc_esher_cfobjective_2012_concurrency_zen.zip'
	ant.delete(file: zip.zipname)
	
	ant.zip( destfile: zip.zipname ){
		fileset( dir: '.', includes: 'build.xml, build.gradle, code/**, preso/*.pptx, preso/*.pdf, preso/*.txt', casesensitive: false )
	}
	copy { 
		from zip.zipname
		into 'C:/Users/marc/Dropbox/CFObjective 2012'
	}
}